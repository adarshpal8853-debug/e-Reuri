[project]
name = "e-reuri"
version = "1.0.0"
description = "Meri Panchayat Django Portal"
requires-python = ">=3.12"

dependencies = [
    "Django==6.0.7",
    "Pillow==12.1.1",
    "psycopg[binary]>=3.1.8",
]

[tool.vercel.scripts]
build = "python manage.py migrate"